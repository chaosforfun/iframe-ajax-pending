{
    "compilerOptions": {
        "baseUrl": "./src", // all paths are relative to the baseUrl
        "experimentalDecorators": true,
        "jsx": "react",
        "lib": [
            "dom",
            "es2019"
        ]
    }
}
